# MICOS-2024 Python Dependencies
# 宏基因组综合分析套件 Python 依赖包
#
# 核心依赖（micos 主链路）：
#   pip install -r requirements.txt
# scripts/ 独立分析脚本依赖：
#   pip install -r requirements-scripts.txt
# 生信工具（fastqc/kraken2/qiime2 等）建议通过 conda 安装：
#   mamba env create -f environment.yml

# === 核心依赖 ===
click>=8.0.0
pyyaml>=6.0
pydantic>=2.0.0

# === 开发与测试 ===
pytest>=6.2.0
pytest-cov>=3.0.0
black>=21.0.0
flake8>=4.0.0
mypy>=0.910
isort>=5.9.0
pre-commit>=2.15.0
